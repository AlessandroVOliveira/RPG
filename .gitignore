# PDFs grandes — o livro do jogador de T20 sozinho tem ~108MB, acima do limite de arquivo do GitHub (100MB).
# As regras relevantes já estão resumidas em memorias/. Remova estas linhas se quiser versionar mesmo assim
# (ou use Git LFS).
manuais-t20/
manuais-classico/

# Lixo comum de SO/editor
.DS_Store
Thumbs.db

# Permissões pessoais desta máquina (não confundir com .claude/settings.json, que é do projeto)
.claude/settings.local.json
